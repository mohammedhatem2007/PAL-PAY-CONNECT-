import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'outcom.palpay.ussd',
  appName: 'PAL PAY CONNECT',
  webDir: 'out' // هذا هو التعديل المطلوب
};

export default config;
